alias sb="source ~/.zshrc"

# git
alias gs="git status"
alias gl="git log"
alias gaca="git commit --all --amend"
alias gpoh="git push origin HEAD"

# eng journal
alias eng_journal="python3 ~/eng_journals/eng_journal.py $1"
